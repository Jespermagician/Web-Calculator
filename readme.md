# Web-Calculator in ~~JS~~ TypeScript

A small Test Projekt. The goal is to make it more complex step for step.

nvm i switch to typescript.

</ hr>

## Setup

`npm init -y`
`npm install typescript --save-dev`

<!-- `npx tsc --init` -->

## Apply Changes

`npx tsc`
